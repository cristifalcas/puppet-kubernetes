[Service]
ExecStart=
ExecStart=-/bin/sh -c "/usr/bin/kube-scheduler \
	    $KUBE_LOGTOSTDERR \
	    $KUBE_LOG_LEVEL \
	    $KUBE_MASTER \
	    $KUBE_SCHEDULER_ARGS 2>&1 | /usr/bin/forward-journald -tag kube-scheduler"
StandardOutput=null
StandardError=null
