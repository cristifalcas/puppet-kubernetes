apiVersion: v1
kind: Node
metadata:
  labels:
    kubernetes.io/hostname: <%= scope.lookupvar("::fqdn") %>
  name: <%= scope.lookupvar("::fqdn") %>
spec:
  podCIDR: <%= scope['kubernetes::node::kubelet_pod_cidr'] %>
